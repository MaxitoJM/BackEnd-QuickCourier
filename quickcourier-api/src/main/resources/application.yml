spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/quickcourier_db
    username: postgres
    password: Maxinene1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: INFO

security:
  jwt:
    # 🔑 Debe tener mínimo 32 bytes (usa una cadena larga y aleatoria)
    secret: "clave-super-segura-y-larga-para-quickcourier-1234567890"
    expiration: 86400000   # 24 horas (en milisegundos)
