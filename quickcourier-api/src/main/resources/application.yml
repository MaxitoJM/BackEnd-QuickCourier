spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quickcourier_db
    username: postgres
    password: Maxinene1
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
