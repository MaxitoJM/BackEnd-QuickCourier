spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/quickcourier_db
    username: postgres
    password: Maxinene1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: INFO

security:
  jwt:
    secret: "cambia-esta-clave-muy-larga-y-segura-al-menos-32bytes........"
    expiration: 86400000 # 24h en ms

